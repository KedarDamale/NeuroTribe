# NeuroTRIBE-HBN — runtime dependencies
# Pinned to compatible ranges; the exact resolved versions are recorded in every
# provenance manifest at run time.

# --- web / API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.12

# --- database ---
SQLAlchemy>=2.0.36,<3.0
alembic>=1.14,<2.0
psycopg[binary]>=3.2,<4.0

# --- workers ---
celery>=5.4,<6.0
redis>=5.2,<6.0

# --- scientific core ---
numpy>=1.26,<3.0
scipy>=1.11,<2.0
pandas>=2.1,<3.0
statsmodels>=0.14,<1.0
scikit-learn>=1.4,<2.0

# --- neuroimaging ---
nibabel>=5.2,<6.0
nilearn>=0.10.4,<1.0
pybids>=0.17,<1.0

# --- utilities ---
PyYAML>=6.0,<7.0
psutil>=6.0,<8.0
pyarrow>=17.0,<21.0
httpx>=0.27,<1.0
gradio_client>=1.10,<2.0
